import { ReactElement } from 'react';

export type Link = {
  id: number;
  name: string;
  href: string;
  icon: ReactElement;
};

export type Skill = {
  id: number;
  name: string;
  icon: ReactElement;
};
